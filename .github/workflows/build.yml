name: Build ESPHome
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build ESPHome
        uses: esphome/build-action@v3
        with:
          yaml-file: xh-s3e-ai.yaml